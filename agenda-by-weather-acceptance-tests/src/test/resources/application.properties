spring.datasource.driverClassName=org.h2.Driver

# Enable SQL logging
spring.jpa.show-sql=true
# Format SQL output to be more readable
spring.jpa.properties.hibernate.format_sql=true

weather-api.points.url=http://localhost:8081/points/{latLon}
geocoding-api.onelineaddress.url=http://localhost:8081/onelineaddress?address={address}